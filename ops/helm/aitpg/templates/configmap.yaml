apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aitpg.fullname" . }}-config
  labels:
    {{- include "aitpg.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
